---
import { Image } from "astro:assets";
import icon_menu from "../assets/icons/icon-menu.png";
---

<header class="container">
  <div class="banner">
    <div class="title">
      <h1 class="main">Montessori Garden</h1>
      <h1 class="sub">Kentish Town</h1>
    </div>
    <button type="button" class="menu-btn">
      <Image
        class="menu-icon"
        src={icon_menu}
        alt="menu button"
        width={361}
        height={216}
      />
    </button>
  </div>
  <nav class="nav" id="nav">
    <ul class="links">
      <li class="link">Home</li>
      <li class="link link--hover js-link--hover">
        Our Nursery
        <ul class="sublinks">
          <li class="link">The Montessori Method</li>
          <li class="link">Curriculum</li>
          <li class="link">A Typical Day</li>
          <li class="link">Our Qualified Staff</li>
        </ul>
      </li>
      <li class="link">Enrollment</li>
      <li class="link">News & Events</li>
      <li class="link">Contact</li>
      <li class="link">Visit Us</li>
      <li class="link">Parents' Corner</li>
      <li class="link">Staff Area</li>
    </ul>
  </nav>
</header>

<script>
  const nav = document.getElementById("nav");
  const button = document.querySelector(".menu-btn");
  const expandingLink = document.querySelectorAll(".js-link--hover");

  function toggleNavExpand() {
    nav?.classList.toggle("nav--expanded");
  }

  function onNavClose() {
    nav?.classList.remove("nav--expanded");
  }

  function toggleLinkExpand(e: Event): void {
    const target = e.currentTarget as HTMLElement;
    target.children[0].classList.toggle("sublinks--expanded");
  }

  function onLinkClose(): void {
    const target = document.querySelector(".sublinks");
    target?.classList.remove("sublinks--expanded");
  }

  if (button === null || nav === null) {
    throw Error;
  } else {
    button.addEventListener("click", toggleNavExpand);
  }

  if (expandingLink === null) {
    throw Error;
  } else {
    expandingLink.forEach((el) => {
      el.addEventListener("click", toggleLinkExpand);
    });
  }

  window.addEventListener("scroll", onNavClose);
  window.addEventListener("scroll", onLinkClose);
</script>

<style lang="scss">
  @import "../styles/vars.scss";

  .container {
    width: 100vw;
    height: $ht-header;
    background-color: white;

    .banner {
      justify-content: space-between;
      display: flex;
      height: 100%;
      padding: 0 1em 0 2em;

      .logo {
        width: 17%;
      }

      .title {
        justify-content: center;
        flex-direction: column;
        display: flex;
        color: $green;

        .main {
          align-items: center;
          display: flex;
          height: 60%;
          font-size: 2.25rem;
          font-family: "Caveat";
          font-weight: 600;
        }

        .sub {
          align-items: center;
          display: flex;
          height: 40%;
          margin-top: -8px;
          font-size: 1.25rem;
          font-family: "Mukta";
          font-weight: 500;
        }
      }

      .menu-btn {
        justify-content: center;
        display: none;
        align-items: center;
        display: flex;
        border: none;
        background: none;

        .menu-icon {
          width: 30px;
          height: 25px;
        }
      }
    }

    .nav {
      z-index: 2;
      position: absolute;
      overflow: hidden;
      transition: height 400ms;
      border-top: 2px solid $green;
      background-color: white;
      width: 100vw;
      height: 0;
      color: $green;

      .links {
        z-index: 1;
        position: aboslute;
        float: right;
        flex-direction: column;
        display: flex;
        gap: 1em;
        width: 50%;
        padding: 1em 1.5em;

        .link {
          justify-content: flex-end;
          align-items: center;
          display: flex;
          height: 1.5em;
        }

        .link--hover:before {
          content: "←";
          align-items: center;
          display: flex;
          margin-right: 1em;
        }

        .sublinks {
          z-index: 0;
          position: absolute;
          right: 50%;
          top: 0;
          gap: 1em;
          flex-direction: column;
          display: flex;
          overflow: hidden;
          transition: width 400ms, padding 400ms ease-out;
          width: 0;
          padding: 1em 0;
          white-space: nowrap;
        }

        .sublinks--expanded {
          transition: width 400ms, padding 400ms ease-out;
          padding: 1em 0.5em;
          width: 50%;
          white-space: nowrap;
        }
      }
    }

    .nav--expanded {
      height: 45vh;
      transition: height 400ms;
    }
  }
</style>
