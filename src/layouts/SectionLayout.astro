---
interface Props {
  title?: string;
  bgColor?: string;
}

const { title, bgColor = "white" } = Astro.props;
---

<section class="container">
  <h1>{title}</h1>
  <slot />
</section>

<style lang="scss" define:vars={{ bgColor }}>
  @import "../styles/vars.scss";

  .container {
    padding: 0 1rem;
    background-color: var(--bgColor);

    h1 {
      padding: 2rem 0 1rem;
      font-size: 2rem;
    }
  }
</style>
