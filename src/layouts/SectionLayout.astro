---
interface Props {
  heading?: string;
  headingColor?: string;
  bgColor?: string;
  isHeadingImg?: string;
}

const {
  heading,
  bgColor = "white",
  headingColor = "white",
  isHeadingImg,
} = Astro.props;
---

<section class="container">
  <h1 class="heading">{heading}</h1>
  <slot />
</section>

<style lang="scss" define:vars={{ bgColor, headingColor, isHeadingImg }}>
  @import "../styles/vars.scss";

  .container {
    margin: 0 auto;
    padding: 1em;
    background-color: var(--bgColor);
    max-width: $xl;

    @media only screen and (min-width: $md) {
      padding: 4rem;
    }

    @media only screen and (min-width: $lg) {
      font-size: 18px;
    }

    .heading {
      font-family: $font-h1;
      font-size: 2rem;
      margin: 1em 0;

      @media only screen and (min-width: $md) {
        height: 2em;
        display: flex;
        align-items: flex-end;
        background-image: url("../assets/images/bg-heading2.png");
        background-position: center;
        background-size: cover;
        color: var(--headingColor);
        font-size: 2.5em;
        margin: 0 0 1em;
        padding-left: 8px;
      }
    }
  }
</style>
