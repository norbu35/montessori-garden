---
const { url, date, title } = Astro.props;
---

<div class="container">
  <a href={url}>
    <div class="header">
      <div class="date">{date}</div>
      <div class="title">{title}</div>
    </div>
    <div class="body">
      <slot />
      [...read more]
    </div>
  </a>
</div>

<style lang="scss">
  @import "../styles/vars.scss";

  .container {
    margin-top: 2em;
    border: 1px solid $green;

    & a,
    & a:visited {
      text-decoration: none;
      color: black;
    }

    .header {
      width: 100%;
      flex-direction: column;
      justify-content: flex-end;
      display: flex;
      border-bottom: 1px solid #32856f;
      padding: 0.5em;
      padding-left: 7px;
      font-size: 1.25em;
      font-family: $font-h1;

      .date {
        margin-bottom: 0.5em;
        font-size: 0.85em;
      }
    }

    .body {
      width: 100%;
      padding: 1em 0.5em;
      font-family: $font-p;
    }
  }
</style>
